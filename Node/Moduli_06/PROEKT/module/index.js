const rand = require("./randoms");//имя файла
const randPi = require("./randomePi");//имя файла

module.exports = {
    //используем имя в кавычках
    "randomNamber" : rand,
    "randomPi" : randPi
}
